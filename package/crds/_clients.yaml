---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: clients.
spec:
  group: ""
  names:
    kind: Client
    listKind: ClientList
    plural: clients
    singular: client
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: ""
    schema:
      openAPIV3Schema:
        description: A Client manages a Keycloak OIDC client.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClientSpec defines the desired state of a Client.
            properties:
              forProvider:
                description: ClientParameters defines the desired state of a Keycloak
                  Client.
                properties:
                  baseURL:
                    description: BaseURL is the client base URL.
                    type: string
                  clientId:
                    description: ClientID is the OAuth2 client ID.
                    type: string
                  description:
                    description: Description is the client description.
                    type: string
                  enabled:
                    default: true
                    description: Enabled enables the client.
                    type: boolean
                  name:
                    description: Name is the display name of the client.
                    type: string
                  realm:
                    description: Realm is the Keycloak realm name.
                    type: string
                  rootURL:
                    description: RootURL is the client root URL.
                    type: string
                  validRedirectURIs:
                    description: ValidRedirectURIs is the list of valid redirect URIs.
                    items:
                      type: string
                    type: array
                  webOrigins:
                    description: WebOrigins is the list of web origins.
                    items:
                      type: string
                    type: array
                required:
                - clientId
                - realm
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ClientStatus defines the observed state of a Client.
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
